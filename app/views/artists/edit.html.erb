<%= form_for @artist do |f| %>
  <h1> Creating an Artist </h1>
  <label> Artist Name: </label>
  <%= f.text_field :name %>
  <label> Artist Bio: </label>
  <%= f.text_field :bio %>
  <br />
  <%= f.submit %>
<% end %>
