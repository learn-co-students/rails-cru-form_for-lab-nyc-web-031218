<%= form_for @artist do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: "Full Name"%>
  <%= f.label :bio %>
  <%= f.text_field :bio, placeholder: "Bio" %>
  <%= f.submit %>
<% end %>
